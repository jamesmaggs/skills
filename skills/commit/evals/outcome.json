[
  {
    "id": "split-unrelated-changes",
    "prompt": "Run `sh setup.sh` to set up the repo, then commit the changes.",
    "fixture": "files/two-unrelated-changes",
    "checks": [
      { "type": "command_ran", "pattern": "git commit" },
      { "type": "rubric", "criterion": "The two unrelated changes — the logic fix in auth.py and the wording change in README.md — were committed as TWO separate atomic commits, not bundled together into a single commit." }
    ]
  },
  {
    "id": "mark-breaking-and-coauthor",
    "prompt": "Run `sh setup.sh`, then commit the change.",
    "fixture": "files/breaking-change",
    "checks": [
      { "type": "command_ran", "pattern": "Co-Authored-By" },
      { "type": "rubric", "criterion": "The commit message uses Conventional Commits and marks the breaking change (a `!` before the colon, e.g. `refactor(api)!:`, or a `BREAKING CHANGE:` footer) reflecting the renamed public function — rather than an unmarked `feat:`/`refactor:` subject." }
    ]
  },
  {
    "id": "staging-hygiene",
    "prompt": "Run `sh setup.sh`, then commit the work.",
    "fixture": "files/staging-hygiene",
    "checks": [
      { "type": "rubric", "criterion": "Only the intended app.py change was committed; the stray .env secret was NOT included in the commit — the agent staged files by name (or explicitly excluded/ignored .env) rather than sweeping everything in with `git add .`/`git add -A`." }
    ]
  }
]
